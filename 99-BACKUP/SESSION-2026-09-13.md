# SESSION BACKUP — 2026-09-13

## 1. Recovery identity

- Repository: `thboulevart-creator/ADAPTIVE-TRADING-DECISION-SYSTEM`
- Governed branch: `feat/v4-3-instrument-contracts`
- PR: #8, draft, unmerged
- Main baseline: `ff50b6d5d123969e091b5df18c46d438f7cb8052`
- Current governed workstream: Phase 3 → 3.1 Expert Momentum
- 3.1.1 verdict: PASS, definition only
- Next governed block: 3.1.2 — Premier backtest baseline

## 2. User's explicit recovery requirement

The project must not lose durable context between sessions. The backup layer must preserve not only conclusions but the useful project context needed to continue: code state, decisions, hypotheses, experiments, evidence, failures, corrections, locked verdicts, and the exact next action.

A new morning session must consult the durable GitHub memory before doing research or searching for yesterday's state.

Do not use conversational recollection as the primary recovery mechanism.

## 3. Locked qualification state — DO NOT RERUN

The following work is already qualified and locked unless genuinely new evidence invalidates it:

- B02 PASS
- B03 / B03.1 / B03.2 / B03.3 historical failures and explanations preserved in the checkpoint
- B02' PASS
- B03.1' PASS
- B04 PASS
- B05 PASS
- B06-A PASS
- B07-A PASS
- B07-B PASS
- B06-B PASS
- B08 PASS
- B08-A PASS
- B08-B.1 PASS
- B09.0.1 PASS
- B09.1 PASS
- B09.2 PASS
- B09.3 PASS after the hash-representation issue was isolated
- B09.4 BLOCKED at the time because numeric representation semantics were not defined
- B09.5 PASS
- B09.6 PASS
- B09.7 synthetic PASS
- B09.7 full-stream PASS
- B09 run() exposure closure PASS
- B09 final PASS

Explicit prohibition: do not rerun B02–B08 or B09.7 merely to reconstruct memory.

## 4. Important evidence identities

- B05 corpus SHA256: `868a21c6a1bedf095b30bc64b6c2ef60b5db9d30146ac53034360254413f8ad7`
- B05 contract SHA256: `49e272534bf5061522eb624afe4e27d6585f730e0271b5284bff2442d5c04b07`
- Historical B08 stream SHA256: `4768c0e66647a15ba703d2dcdc88c03db84846a625e8eb3f70914428bf11c8d7`
- B09 canonical stream SHA256: `d8da494b2a1380ea0db0e0370ece4f609374ecb3e5647b6c1fb290836867abda`
- Corpus: `C:\ALGO-DATA\qualification\v4_3_multi_year_acquisition\candidate_b03_3_repaired\USATECHIDXUSD`
- Physical files: 495
- Full stream: 5,130,393 ticks

## 5. B08-A/B09 code persistence incident

The checkpoint correctly records that the following production research files were described by the historical work but were not present on the governed GitHub branch when checked:

- `src/research/input_binding.py`
- `src/research/bi5_reader.py`
- `src/research/engine.py`
- `src/research/execution.py`

The current GitHub branch still does not contain these four files. Therefore their current local persistence remains unresolved until the exact Windows worktree is accessible and inspected.

Historical exact worktree path:

`C:\Users\Boulevart\Documents\ADAPTIVE-TRADING-DECISION-SYSTEM-PR8-TEST`

No claim is made that these files are currently recoverable locally until that filesystem is actually inspected.

## 6. B08-A/B09 architecture that must be preserved if code is recovered or reconstructed

`BoundResearchInput` is the capability boundary. It is immutable and may only be created by `bind_execution_input()`.

`BI5ResearchEngine` must accept only `BoundResearchInput`; direct unbound construction is rejected.

The official production execution path is:

`run_qualified_research()` → `bind_execution_input()` → `BI5ResearchEngine(bound_input)` → `_execute_bound_stream()` → `engine.iter_ticks()`.

`BI5ResearchEngine.run()` is classified `NON_OFFICIAL_NON_PRODUCTION` and is not admissible as the qualification entrypoint.

The BI5 reader must not silently sort ticks, deduplicate, repair, interpolate, substitute or otherwise mutate the data. File ordering is deterministic and duplicate hours are rejected. Global tick monotonicity is enforced on the official iterator path.

B09 changed the stream signature from the historical decimal representation to exact IEEE-754 binary serialization using `struct.pack(">d", value)` for the numeric tick fields. This change is why the canonical B09 stream SHA differs from the historical B08 SHA.

## 7. Momentum 3.1.1 locked definition

- Timeframe: H1
- Variable: Close
- Horizon: 20 bars
- `M_t = Close_t / Close_{t-20} - 1`
- `M_t > 0` → LONG
- `M_t < 0` → SHORT
- `M_t = 0` → NEUTRE
- insufficient history → UNDEFINED
- calculated at close of `t`
- usable from `t+1`
- no regime filter
- no massive optimization
- signal is directional, not an order
- risk, SL/TP and execution are outside the 3.1.1 definition

3.1.1 PASS validates only the formal definition, not profitability, robustness, economic value or regime superiority.

## 8. Current task boundary

The current task is still recovery/persistence first.

Only after the B08-A/B09 code state is resolved may the project proceed to:

`3.1.2 → formalisation du protocole de premier backtest baseline → cassage → correction → re-cassage → validation → exécution.`

No B02–B08 rerun. No B09.7 rerun.

## 9. Durable backup policy established today

`99-BACKUP/` is now the dedicated durable project-memory directory.

Every material work session must create/update a dated session snapshot containing the context required to recover the work without relying on conversation history.

The README in this directory defines the morning recovery order.

The Recovery Checkpoint remains the authoritative current-state pointer. This backup directory preserves the broader context so that the checkpoint never becomes the only surviving description of the work.

## 10. Failure-prevention rules

- Never declare code saved unless it exists on GitHub or its exact local persistence is explicitly recorded.
- Never confuse a checkpoint describing code with the code itself.
- Never silently recreate missing validated work and call it the original implementation.
- If code is lost, distinguish reconstruction from recovery.
- If a required artifact cannot be found after all accessible sources are checked, state `NOT FOUND / BLOCKED` and create a controlled replacement only after recording that fact.
- Never scatter competing backups across arbitrary folders or branches.
- Use `99-BACKUP/` as the durable context layer for future sessions.
